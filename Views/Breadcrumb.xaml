<UserControl x:Class="ColumnFinder.Views.Breadcrumb"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="Transparent" MouseLeftButtonUp="Root_MouseLeftButtonUp">
        <ItemsControl x:Name="Items" VerticalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="{Binding Name}"
                                Tag="{Binding Path}"
                                Click="Crumb_Click"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="6,2"
                                Cursor="Hand"/>
                        <TextBlock Text="›" VerticalAlignment="Center" Margin="2,0" Foreground="#888"
                                   Visibility="{Binding SeparatorVisibility}"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBox x:Name="Editor"
                 Visibility="Collapsed"
                 BorderThickness="0" Background="Transparent"
                 VerticalAlignment="Center"
                 KeyDown="Editor_KeyDown"
                 LostFocus="Editor_LostFocus"/>
    </Grid>
</UserControl>
